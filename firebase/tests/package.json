{
  "name": "kitchenai-firestore-rules-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Emulator tests for firebase/firestore.rules",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project demo-kitchenai-rules --only firestore --config ../../firebase.json \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
